// Stub: iMessage removed from AnrakPilot fork.
export function parseIMessageTarget(_target: string): { handle?: string } | null {
  return null;
}

export function normalizeIMessageHandle(handle: string): string {
  return handle;
}
