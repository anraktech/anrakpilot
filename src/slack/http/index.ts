// Stub: Slack removed from AnrakPilot fork.
import type { IncomingMessage, ServerResponse } from "node:http";

export function handleSlackHttpRequest(_req: IncomingMessage, _res: ServerResponse): boolean {
  return false;
}
